{% extends '@Espritsocialpro/layout.html.twig' %}



{% block titreheader %}
    Département
{% endblock %}
{% block active3 %} class="sub-menu active" {% endblock %}
    {% block active32 %} class="active" {% endblock %}{% block descheader %}

{% endblock %}

{% block bodyheader %}

{% endblock %}

{% block bodymain %}


        <div class="card">
<div class="pull-right">
            <div class="card-header">
                <p>


                <button class="btn palette-Teal bg"><i class="zmdi zmdi-plus-circle zmdi-hc-fw"></i> Ajouter</button>
                </p>
            </div>
</div>
            <table id="data-table-command" class="table table-striped table-vmiddle">
                <thead>
                <tr>
                    <th data-column-id="id" data-type="numeric">ID</th>
                    <th data-column-id="departement">departement</th>
                    <th data-column-id="chef de departement" data-order="desc">Chef departement</th>
                    <th data-column-id="commands" data-formatter="commands" data-sortable="false">Commandes</th>
                </tr>
                </thead>
                <tbody>
                {% for x in departements %}
                    <tr>
                        <td>{{ x.iddepartement}}</td>
                        <td>{{ x.nomDepartement }}</td>
                        <td>{{ x.chef.username }}</td>

                        <td><a  href="{{ path('modifier_departement',{'id':x.iddepartement}) }}" class='btn palette-Light-Green bg btn-icon waves-effect waves-circle waves-float'><span class='zmdi zmdi-edit'></span></a>
                            <a  href="{{ path('supp_departement',{'id':x.iddepartement}) }}" class='btn btn-danger btn-icon waves-effect waves-circle waves-float'><span class='zmdi zmdi-delete'></span></a></td>

                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    {% block tabscript %}
        {% endblock %}
{% endblock %}
